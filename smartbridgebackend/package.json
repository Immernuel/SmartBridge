{
  "name": "smartbridge-registry-backend",
  "version": "1.0.0",
  "description": "SmartBridge registry — stores DEX wallet to CEX credential mappings",
  "type": "module",
  "scripts": {
    "dev": "node --watch --experimental-strip-types registry.ts",
    "start": "node --experimental-strip-types registry.ts"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.25",
    "@types/node": "^22.0.0"
  },
  "engines": {
    "node": ">=22.6.0"
  }
}
